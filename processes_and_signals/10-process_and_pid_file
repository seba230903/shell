#!/usr/bin/env bash
#hola
echo $$ > /var/run/myscript.pid
trap 'echo Y U no love me' SIGINIT
trap 'echo I hate the kill command' SIGTERM
trap 'rm /var/run/myscript.pid || exit' SIGTERM SIGQUIT
while true
do
	echo "To infinity and beyond"
	sleep 2
done
